using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using TweetSharp;

namespace trc2
{
    class UtilityClass
    {
/*     
         static public void CheckResult(RequestResult result, string ErrMessage)
        {
            switch (result)
            {
                case RequestResult.Success:
                    break;
                case RequestResult.ConnectionFailure:
                case RequestResult.TwitterIsDown:
                case RequestResult.TwitterIsOverloaded:
                    throw new ApplicationException("一時的に利用できません。");
                case RequestResult.Unauthorized:
                    throw new ApplicationException("認証にエラーがあります。");
                case RequestResult.RateLimited:
                    throw new ApplicationException("規制に引っ掛かっています。");
                default:
                    throw new ApplicationException("不明なエラー:" + ErrMessage);
            }
        }
    */
    }
}
